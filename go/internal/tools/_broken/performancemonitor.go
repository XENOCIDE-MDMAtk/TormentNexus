package tools

import (
	"context"
)

func HandleWaitStats(ctx context.Context, args map[string]interface{}) (ToolResponse, error) {
	return err("database connectivity not implemented in Go version without external packages")
}