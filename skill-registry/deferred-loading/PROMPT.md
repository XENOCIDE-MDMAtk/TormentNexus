# Task: Progressive Loading for Skills

## Goal
Implement a progressive loading and disclosure scheme for the skill registry.

## Key Requirements
- load only the descriptive skill names initially.
- load frontmatter progressively based on predictive conversation analysis (predict what is needed next).
- load full skill content only when explicitly invoked by the model.
